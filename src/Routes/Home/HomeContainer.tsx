import React from "react";
import styled from "styled-components";

const Container = styled.div`
  height: 300vh;
`;

const HomeContainer: React.FC = () => {
  return <Container>홈화면</Container>;
};

export default HomeContainer;
